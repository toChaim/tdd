# tdd
Build a node server using TDD.

#instalation
1. download repository
2. npm install
3. npm start

# terminal commands
"npm start" to start server.
"npm test" to run tests.
"npm run test-with-coverage" to run test and see coverage report.